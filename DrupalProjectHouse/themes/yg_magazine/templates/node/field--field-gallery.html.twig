{% for item in items %}  
    <div class="col-md-4 col-sm-12 col-xs-12">
      <div class="hovereffect">
        <img src="{{ file_url(item.content['#item'].entity.uri.value) }}" class="img-responsive">
      </div>
    </div>
{% endfor %}

